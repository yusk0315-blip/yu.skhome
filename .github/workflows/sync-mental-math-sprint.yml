name: Sync mental math sprint card

on:
  push:
    branches: [main]
    paths:
      - 'apps/mental-math-sprint/**'
      - 'scripts/sync_mental_math_sprint_card.py'
      - '.github/workflows/sync-mental-math-sprint.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-card:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python scripts/sync_mental_math_sprint_card.py
      - name: Commit home card
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "暗算スプリントをホームに追加"
          git push
